Сырой вариант проекта, поэтому, для экономии своего времени, пока воздержитесь от погружения в логику)

На созвоне обсуждали использование такого рода интерфейса. Вроде исправил, но лучше посмотрите еще разок:

/*
    type Collection interface {
        GetRemovedIDs() []int
        ClearRemovedList()

        GetNewItems() interface{}
        RemoveItemsWithoutID()
        AddOrUpdateItems(interface{}) error

        GetAllIDsWithModtime() map[int]time.Time
        GetItems([]int) interface{}
        RemoveItems([]int)

        Clear()
    }
*/

Хотелось бы получить небольшой обзор моих решений.
Какие из них вы считаете верными, какие спорными, какие требуют доработки, а от каких необходимо полностью отказаться (как пример выше).
Если позволит время, то можете опуститься даже до подобного: "слишком длинные функции", "Здесь излишняя дублируемость кода" и т.п.